# CLI-password-manager
This program is a command-line interface (CLI) application written in C. It allows users to securely store and retrieve passwords for different websites. To ensure privacy, the passwords are encrypted before being written to a text file and decrypted only when retrieved by the user.
